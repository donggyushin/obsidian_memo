> source: https://www.youtube.com/watch?v=vxEvo2BLM6A

# Claude Code 학습 노트

## CLAUDE.md 운영

- 파일이 너무 커지면 **토큰 사용량이 증가**하므로 폴더별로 분할해 관리하는 것이 좋다.
- 수동으로 직접 수정할 필요는 없다. 클로드에게 "자동으로 업데이트해줘"라고 요청하는 방식이 가장 편하다.

## Trigger Keywords(그냥 Command 쓰는 게 더 좋음)

![[스크린샷 2026-04-17 오전 10.28.02.png]]

## Compound Engineering

![[스크린샷 2026-04-17 오전 10.29.53.png]]

## 핵심 단축키

| 단축키 | 동작 |
| --- | --- |
| `Shift + Tab` | Plan Mode ↔ Accept Mode 전환 |
| `Escape` | 즉시 중단 |
| `Escape × 2` | 입력 삭제 / 복원 |

## Plan Mode를 써야 하는 실무적 이유

1. **토큰 최적화** — "잘못된 코드 생성 → 수정 요청 → 재생성" 악순환을 끊을 수 있다.
2. **컨텍스트 윈도우 보존**
3. **실행 정확도 향상**
4. **비용 관리** — input / output 중 output 토큰을 절약할 수 있다.

> [!tip] 결론
> **Plan → 검토 → Execute** 패턴을 습관화하자.

### 이미지를 넘겨주는 거 좋음

### `!` 접두사로 bash 명령어 직접 실행 가능

### 컨텍스트 관리 커맨드

1. `/clear`
2. `/compact`
3. `/context`
    1. 바(bar) 형태로 현재 사용량 보여줌
4. `/model`
    1. 병렬로 서브에이전트들을 많이 다룰 때는 Sonnet 사용하는 게 좋음
5. `/resume`
6. `/mcp`
    1. supabase, notion 추천
    2. 양날의 검임. 아무것도 안 해도 시작부터 Context를 어느 정도 차지하고 싶어함.
    3. mcp를 무자비하게 사용하는 건 비추함.
    4. custom mcp 만들어서 사용하는 게 제일 좋음
7. `/export`
    1. 컨텍스트를 파일로 추출해줌
8. `/output-style`

## 컨텍스트 관리

### 컨텍스트는 왕이다

#### 세컨드 브레인 구축

의사결정의 이유 등을 로컬 마크다운 파일에 저장해야 함. 수동으로 관리할 필요가 없음. `/memory` 라는 커맨드로 자동으로 해줌. 알아서 기억함. 간단하게 클로드에게 기억해줘 라고 말하기만 하면 됨.

#### LazyLoading

`claude.md` 하나에 몰아넣으면 안 됨. `claude.md`는 매번 읽어오는 데이터이기 때문. `claude.md`는 규칙과 참조만, 상세 내용은 별도 파일로 분리. `claude.md`는 참조하는 마크다운 파일을 file path로만 기억해두기.
클로드가 필요할 때만 해당 파일을 읽게 하기.
여러 개의 `claude.md`를 폴더별로 따로 만들어서 관리하는 것도 좋음. 핵심은 Root에 `Claude.md`를 너무 비대하게 두지 않는다.

#### 하나의 세션에서는 하나의 피쳐만 하자.

작은 단위로 잘 쪼개서 작업하는 게 중요함.

![[스크린샷 2026-04-17 오후 3.08.37.png]]
멀티플터미널 활용. 서브 에이전트. 에이전트 팀즈 활용.

### Mermaid 아키텍쳐 관리

![[스크린샷 2026-04-17 오후 3.11.06.png]]
아키텍쳐를 글이 아닌 image로 설명하면 claude가 훨씬 잘 이해할 수 있음.

## 워크플로우 & 코딩 철학

### Plan Mode 먼저

작업설명 → 계획 제시 → 리뷰 & 피드백 → 실행.

### TDD 기반 스마트 코딩

작은 변경 → 테스트 → 린트 → 커밋 → 작은 변경.

### Thinking log 읽기

잘못된 판단 발견하면 바로 Escape.

### 다른 AI에게 비평 받기

export해서 다른 AI에게 비평 받기.

### 에러 로그 그대로 붙여넣기

> [!warning] 주의
> 절대 에러로그를 해석해서 클로드에게 주지 마세요.

### 로컬 TODO md 파일 적극 활용

### WAT 프레임워크

Workflows, Agents, Tools.
작업 흐름을 정의 → AI가 스스로 판단하고 복구 → 도구 조합.

#### Workflows

todo.md 파일 활용.

#### Agents

![[스크린샷 2026-04-17 오후 3.20.42.png]]

#### Tools

![[스크린샷 2026-04-17 오후 3.21.17.png]]

## Skills

AI에게 주는 업무 매뉴얼.

![[스크린샷 2026-04-17 오후 5.20.38.png]]
![[스크린샷 2026-04-17 오후 5.21.24.png]]
![[스크린샷 2026-04-17 오후 5.23.32.png]]

간단한 작업이라면 MCP 사용보다는 local skill로 두고 쓰는 게 좋음. skill은 사용하지 않을 때 공간을 거의 차지하지 않음.

![[스크린샷 2026-04-17 오후 5.28.40.png]]
![[스크린샷 2026-04-17 오후 5.29.01.png]]

skill-creator plugin 사용해서 원하는 스킬을 만들기.

## Sub-Agents & 병렬 실행

서브에이전트의 좋은 점은 메인에이전트의 context를 차지하지 않음. 별도의 컨텍스트를 가지기 때문에 실행 중에 가장 효율적임. opus 사용을 권장하지 않음
![[스크린샷 2026-04-17 오후 6.21.40.png]]
/agents 명령어로 생성하는게 가장 좋음. 

## Agent Teams


## Hooks

## 고급 팁
